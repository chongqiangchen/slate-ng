import { PlaceholderAttrsDirective } from './placeholder-attrs.directive';

describe('PlaceholderAttrsDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderAttrsDirective();
    expect(directive).toBeTruthy();
  });
});
