import { ElementAttrsDirective } from './element-attrs.directive';

describe('ElementAttrsDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementAttrsDirective();
    expect(directive).toBeTruthy();
  });
});
