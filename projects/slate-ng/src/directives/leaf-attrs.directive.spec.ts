import { LeafAttrsDirective } from './leaf-attrs.directive';

describe('LeafAttrsDirective', () => {
  it('should create an instance', () => {
    const directive = new LeafAttrsDirective();
    expect(directive).toBeTruthy();
  });
});
